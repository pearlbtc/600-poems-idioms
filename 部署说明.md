# 《每天进步一点点》PWA 部署说明

本文件夹是一个**已配置好 PWA** 的纯静态网站，可直接托管到任意支持 HTTPS 的静态平台，手机打开即可「添加到主屏幕」变成 App。

## 文件清单
- `index.html` / `app.js` / `styles.css` / `data.js`：应用主体
- `manifest.webmanifest`：PWA 配置（全屏/竖屏/图标）
- `sw.js`：离线缓存（核心资源预缓存，音频首次播放后自动缓存）
- `icons/`：应用图标（192 / 512 / maskable）
- `assets/`：封面与卷首图
- `audio/`：央视风诵读音频
- `.nojekyll`：防止 GitHub Pages 用 Jekyll 处理（保持文件原样）

## 方式一：Netlify Drop（最省事，30 秒）
1. 打开 https://app.netlify.com/drop
2. 把**整个文件夹**拖进去
3. 立刻得到一个 `https://xxxx.netlify.app` 的 HTTPS 链接
4. 手机 Chrome/Edge 打开 → 右上角 `⋮` → **添加到主屏幕**

## 方式二：Vercel
1. 打开 https://vercel.com → 登录（GitHub/邮箱）
2. `Add New → Project` → 选「Deploy from Local」或直接拖文件夹
3. 得到 `https://xxxx.vercel.app` HTTPS 链接，手机即可安装

## 方式三：GitHub Pages（永久、自己的域名）
1. 在 GitHub 新建一个仓库（如 `meitian-pwa`）
2. 把本文件夹所有内容 push 上去（含 `.nojekyll`）
3. 仓库 `Settings → Pages` → Source 选 `main` 分支根目录 → Save
4. 几分钟后得到 `https://你的用户名.github.io/meitian-pwa/` HTTPS 链接
5. 手机打开即可安装

## 方式四：腾讯云 COS + CDN（国内访问快）
1. 建一个 COS 存储桶，开启「静态网站托管」
2. 把本文件夹全部上传
3. 绑定自定义域名并开启 HTTPS（CDN 证书）
4. 手机访问该域名即可安装

## 本地临时预览（不安装，仅看效果）
电脑和手机连同一 WiFi，手机浏览器访问：
- `http://192.168.0.103:8200`（电脑 WiFi IP，可能随网络变化，以 `ipconfig` 查到的为准）
> 注意：局域网 `http://` 下 Service Worker 不生效（离线缓存失效），且 **iOS 必须 HTTPS 才能装 App**；安卓 Chrome 可试装。正式安装请用上面任一 HTTPS 平台。

## 安卓真机 .apk（不依赖浏览器）
见 `android-app/README.md`：用 Android Studio 打开 `android-app` 工程 → Build APK → 手机安装 `app-debug.apk`。
